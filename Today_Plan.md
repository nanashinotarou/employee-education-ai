# Today_Plan.md — Day12 実装計画

> **最終更新**: 2026-04-28 by Antigravity (Gemini Pro)
> **担当**: ClaudeCode (Claude Opus 4.7) 向け実装計画書
> **対象**: `vol12-1.html` の新規作成

---

## 📌 ClaudeCodeへの指示

AntigravityによるDay12の動画リサーチが完了しました（`Today_Research.md` 参照）。
このリサーチ結果と、既存のファイル（`vol11-1.html` や `index.html`、および `GEMINI.md` に記載のルール）を参照し、**Cursorで一発実装できる詳細な実装計画（マークダウン形式）** にこのファイルを書き換えてください。

### 必須要件の確認事項（実装計画に必ず含めること）
1. **ベースとなる構成:**
   - 既存の `vol11-1.html` をコピー（または同等の構造を流用）して `vol12-1.html` を作成する手順。
   - ページタイトル、ヒーローセクションのタイトル（Day12 AI活用によるアプリケーション開発実践②）の更新。
2. **コンテンツの構成:**
   - 本日の目標
   - 前半（動画2本 + 実習内容）
   - 後半（動画2本 + OPALリンク + 実習内容）
   - 今日のまとめ
3. **GEMINI.md ルールの徹底:**
   - **YouTube Facadeパターン:** iframeの直接記述は避け、`.vc-thumb` による遅延読み込みと `maxresdefault.jpg` のフォールバックを設定する。
   - **モバイルレイアウト最適化:** `@media (max-width: 768px)` のCSSを必ず末尾に含める。
4. **ナビゲーション:**
   - 各パートの最下部に、「次パートへ」や「ホーム（`index.html`）へ戻る」ボタンを配置。

ClaudeCodeは、このファイル（`Today_Plan.md`）の内容を**具体的なHTML構造やCSSの追加指示を含む完全な実装手順書**として上書き更新し、Cursorが迷わず作業できるように整えてください。
